<div class="row">
  <div class="col-sm-6">
  	<img ng-src="{{selectedProduct.image_url}}" alt="{{selectedProduct.name}}" heigth="75%" width="75%" />
	</div>

	<div class="col-sm-4">
		<div class="thumbnail">
      <div class="caption">
	    <h3>{{ selectedProduct.name }}</h3>
        <p ng-bind-html="selectedProduct.short_description"></p>
        <p ng-bind-html="selectedProduct.description"></p>
        <p><a href="#" ng-click="closeDetails()" class="btn btn-primary" role="button">Back</a></p>
      </div>
    </div>
	</div>
</div>